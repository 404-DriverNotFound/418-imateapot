#include <iostream>
#include <fstream>
#include <vector>

void addVector(std::vector< std::vector<int> > &test) {
	std::vector<int> a;
	a.push_back(1);
	test.push_back(a);
}

int main()
{
	
	return 0;
}